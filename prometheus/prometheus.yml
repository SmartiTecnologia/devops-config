global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "otel-collector"

    static_configs:
      - targets:
          - otel-collector:8889
          
  - job_name: "fastapi-app"
    metrics_path: /metrics
    scrape_interval: 5s
    static_configs:
      - targets:
          - urnadigitalback.sandboxcode.link